{
  "name": "astro-payload-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm dev",
    "dev:frontend": "cd frontend && npm dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
